import {PureArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';
import {PluginLayout} from '../pluginLayout';
import {PluginHeader} from '../pluginHeader';
import {PluginFooter} from '../pluginFooter';

<Meta title="Layout/Plugin Layout" of={PluginLayout} />

# Plugin Layout

<Canvas>
  <Story id="layout-plugin-layout--basic" />
</Canvas>

```typescript
import {PluginLayout} from '@frontapp/ui-kit';
```

The `PluginLayout` is a simple layout component that enables a standard layout that most
plugins will want to follow.

<br />

## Props

<PureArgsTable of={PluginLayout} />

<hr />
<hr />

# Plugin Header

```typescript
import {PluginHeader} from '@frontapp/ui-kit';
```

The `PluginHeader` will render the content supplied in the `header` grid-area of the `PluginLayout`.

<br />

## Props

<PureArgsTable of={PluginHeader} />

<hr />
<hr />

# Plugin Footer

```typescript
import {PluginFooter} from '@frontapp/ui-kit';
```

The `PluginFooter` will render the content supplied in the `footer` grid-area of the `PluginLayout`.

<br />

## Props

<PureArgsTable of={PluginFooter} />

<hr />
<hr />

# Stories

## Top-level Plugin Layout

<Canvas>
  <Story id="layout-plugin-layout--basic" />
</Canvas>

This story shows what a layout would look like when you are at a top level page on a plugin.

## Top Level Plugin Layout With Search Input

<Canvas>
  <Story id="layout-plugin-layout--search" />
</Canvas>

This story shows what a layout would look like when you are at a top level page on a plugin and add a search input.

## Sub-level Plugin Layout

<Canvas>
  <Story id="layout-plugin-layout--sub-level" />
</Canvas>

This story shows what a layout would look like when you are at a sub level page on a plugin.
