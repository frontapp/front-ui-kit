import {PureArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';
import {Pill} from '../pill';
import {PillContent} from '../pillContent';
import {PillContentIcon} from '../pillContentIcon';

<Meta title="Components/Pill" of={Pill} />

# Pill

<Canvas>
  <Story id="components-pill--basic" />
</Canvas>

```typescript
import {Pill} from '@frontapp/ui-kit';
```

A pill is a way to render information in a specific format. You can customize all the colors of the pill.

<br />

## Props

<PureArgsTable of={Pill} />

<hr />
<hr />

# Pill Content

```typescript
import {PillContent} from '@frontapp/ui-kit';
```

`PillContent` is a component that wraps the content of a pill and renders it in the correct grid area.
You will not need to use this component as the pill automatically wraps the content with this.

<br />

## Props

<PureArgsTable of={PillContent} />

<hr />
<hr />

# Pill Content Icon

<Canvas>
  <Story id="components-pill--icon-pill" />
</Canvas>

```typescript
import {PillContentIcon} from '@frontapp/ui-kit';
```

`PillContentIcon` is used to easily align icons with the two supported positions we have.
We will ignore all the content of the `PillContentIcon` except for the first `Icon` passed in.

<br />

## Props

<PureArgsTable of={PillContentIcon} />

<hr />
<hr />
