import {PureArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';
import {File} from '../file';

<Meta title="Components/File" of={File} />

# File

<Canvas>
  <Story id="components-file--basic" />
</Canvas>

```typescript
import {File} from '@frontapp/ui-kit';
```

The file component is a visual way to render a File object.

#### Note: We currently do not support passing the Javascript File object directly.

<br />

## Props

<PureArgsTable of={File} />

<hr />
<hr />

# Stories

## Erred File

<Canvas>
  <Story id="components-file--erred" />
</Canvas>

You can change the state of the `File` component to be in an erred state.

## Clear File Action

<Canvas>
  <Story id="components-file--on-clear" />
</Canvas>

If you supply a `onClear` prop we will render a clear button when the `File` is hovered.

(Note: The `Undo clear` is not part of the file, only the example.)
