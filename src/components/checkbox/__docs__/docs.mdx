import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Checkbox } from '../checkbox';

<Meta title="Components/Checkbox" of={Checkbox} />

# Checkbox

<Canvas>
  <Story id="components-checkbox--basic" />
</Canvas>

```typescript
import {Checkbox} from '@frontapp/ui-kit';
```

Checkboxes are used for multiple choices, not for mutually exclusive choices. Each checkbox works independently 
from other checkboxes in the list, therefore checking an additional box does not affect any other selections.

### When not to use a checkbox

If a user can select only one option from a list, radio buttons should be used instead of checkboxes. Checkboxes 
allow the user to select multiple items in a set whereas radio buttons allow the user to select only one option.

<br />

## Props

<ArgsTable of={Checkbox} />

<hr /><hr />

# Stories

## Indeterminate State

<Canvas>
  <Story id="components-checkbox--indeterminate" />
</Canvas>

A checkbox can be in an indeterminate state which is typically used when there are checkboxes that are tied to a parent
checkbox.